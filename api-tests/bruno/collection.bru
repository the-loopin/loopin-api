meta {
  name: Loopin API
  type: http
  seq: 1
}

vars {
  base_url: {{base_url}}
  auth_token: {{auth_token}}
}

docs {
  Loopin backend validation collection for local and staging environments. The collection uses placeholders only; keep real JWTs and environment credentials in your local Bruno workspace.
}
