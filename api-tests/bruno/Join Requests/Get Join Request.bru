meta {
  name: Get Join Request
  type: http
  seq: 3
}

get {
  url: {{base_url}}/groups/{{group_id}}/join-requests/{{join_request_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
