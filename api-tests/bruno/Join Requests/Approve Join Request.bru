meta {
  name: Approve Join Request
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/groups/{{group_id}}/join-requests/{{join_request_id}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
