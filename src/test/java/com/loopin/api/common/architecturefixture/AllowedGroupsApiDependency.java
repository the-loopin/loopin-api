package com.loopin.api.common.architecturefixture;

import com.loopin.api.groups.api.GroupSubscriptionAuthorizer;

/** Deliberately allowed fixture used to prove the Groups boundary rule. */
public class AllowedGroupsApiDependency {
    private GroupSubscriptionAuthorizer groupSubscriptionAuthorizer;
}
