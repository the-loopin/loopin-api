spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

  liquibase:
    contexts: production

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
