spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}

  liquibase:
    contexts: staging

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
